# This function load configuration file

import os
import re
import pandas as pd

def load_param(config_path, skiprows, seperator):
    if os.path.isfile(conf_path):
        with open(config_path,'r') as f:
            config =


